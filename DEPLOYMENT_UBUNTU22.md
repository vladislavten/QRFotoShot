# Инструкция по развертыванию QRShot на Ubuntu 22.04 LTS

## 📋 Содержание
1. [Подготовка сервера](#1-подготовка-сервера)
2. [Установка Node.js](#2-установка-nodejs)
3. [Установка системных зависимостей](#3-установка-системных-зависимостей)
4. [Настройка проекта](#4-настройка-проекта)
5. [Настройка базы данных](#5-настройка-базы-данных)
6. [Настройка веб-сервера (Nginx)](#6-настройка-веб-сервера-nginx)
7. [Настройка HTTPS (Let's Encrypt)](#7-настройка-https-lets-encrypt)
8. [Настройка автозапуска (PM2)](#8-настройка-автозапуска-pm2)
9. [Настройка файрвола](#9-настройка-файрвола)
10. [Проверка работы](#10-проверка-работы)
11. [Управление сервисом](#11-управление-сервисом)
12. [Резервное копирование](#12-резервное-копирование)

---

## 1. Подготовка сервера

### 1.1. Обновление системы

```bash
# Обновляем список пакетов
sudo apt update

# Обновляем систему до последних версий
sudo apt upgrade -y

# Перезагружаем сервер (если требуется)
sudo reboot
```

**⏱️ Время выполнения:** 5-10 минут  
**🔄 Перезагрузка:** Да, если были обновления ядра

### 1.2. Создание пользователя для приложения (опционально, но рекомендуется)

```bash
# Создаем нового пользователя
sudo adduser qrshot

# Добавляем пользователя в группу sudo (если нужны права администратора)
sudo usermod -aG sudo qrshot

# Переключаемся на нового пользователя
su - qrshot
```

---

## 2. Установка Node.js

### 2.1. Установка Node.js 20.x (LTS)

```bash
# Устанавливаем curl (если еще не установлен)
sudo apt install -y curl

# Добавляем официальный репозиторий NodeSource для Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -

# Устанавливаем Node.js
sudo apt install -y nodejs

# Проверяем версии
node --version
npm --version
```

**Ожидаемый результат:**
- Node.js: v20.x.x
- npm: 10.x.x

**⏱️ Время выполнения:** 3-5 минут

### 2.2. Установка PM2 (менеджер процессов)

```bash
# Устанавливаем PM2 глобально
sudo npm install -g pm2

# Проверяем установку
pm2 --version
```

**⏱️ Время выполнения:** 1-2 минуты

---

## 3. Установка системных зависимостей

### 3.1. Установка инструментов для сборки нативных модулей

```bash
# Устанавливаем build-essential и другие необходимые пакеты
sudo apt install -y build-essential python3

# Устанавливаем дополнительные библиотеки для Sharp (обработка изображений)
sudo apt install -y libvips-dev libvips-tools
```

**⏱️ Время выполнения:** 5-10 минут

### 3.2. Установка FFmpeg (для обработки видео, опционально)

```bash
# Устанавливаем FFmpeg
sudo apt install -y ffmpeg

# Проверяем установку
ffmpeg -version
```

**⏱️ Время выполнения:** 2-3 минуты

### 3.3. Установка SQLite (обычно уже установлен)

```bash
# Проверяем наличие SQLite
sqlite3 --version

# Если не установлен, устанавливаем
sudo apt install -y sqlite3
```

---

## 4. Настройка проекта

### 4.1. Создание директории для проекта

```bash
# Создаем директорию для проекта
sudo mkdir -p /var/www/qrshot

# Устанавливаем владельца (замените qrshot на вашего пользователя)
sudo chown -R $USER:$USER /var/www/qrshot

# Переходим в директорию
cd /var/www/qrshot
```

### 4.2. Загрузка проекта

**Вариант 1: Через Git (если проект в репозитории)**

```bash
# Устанавливаем Git (если еще не установлен)
sudo apt install -y git

# Клонируем репозиторий
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> .

# Или если репозиторий приватный, используйте SSH ключ
# git clone git@github.com:username/qrshot.git .
```

**Вариант 2: Загрузка через SCP/SFTP**

```bash
# На локальной машине (Windows/Mac/Linux)
# scp -r /path/to/project user@server_ip:/var/www/qrshot/

# Или используйте FileZilla, WinSCP и т.д.
```

**Вариант 3: Создание структуры вручную**

```bash
# Создаем структуру директорий
mkdir -p backend frontend

# Копируем файлы проекта в соответствующие директории
```

### 4.3. Установка зависимостей проекта

```bash
# Переходим в директорию backend
cd /var/www/qrshot/backend

# Устанавливаем зависимости Node.js
npm install

# Проверяем, что все установилось
ls node_modules
```

**⏱️ Время выполнения:** 5-10 минут (зависит от скорости интернета)

### 4.4. Настройка переменных окружения

```bash
# Создаем файл .env в директории backend
cd /var/www/qrshot/backend
nano .env
```

**Содержимое файла `.env`:**

```env
# Порт сервера
PORT=3000

# URL фронтенда (сначала HTTP, после настройки SSL измените на HTTPS)
FRONTEND_URL=http://qrshot.kz

# JWT секретный ключ (сгенерируйте случайную строку)
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-string

# Время жизни JWT токена
JWT_EXPIRES_IN=30m

# Время простоя перед выходом (в миллисекундах)
IDLE_TIMEOUT_MS=1800000

# Данные администратора
ADMIN_USER=admin
ADMIN_PASS=your-secure-password-change-this

# Telegram Bot Token (опционально, для уведомлений)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Длительность автоматического завершения мероприятия (в миллисекундах)
EVENT_AUTO_END_DURATION_MS=43200000
```

**Генерация безопасного JWT_SECRET:**

```bash
# Генерируем случайную строку для JWT_SECRET
openssl rand -base64 32
```

**Сохранение файла:**
- Нажмите `Ctrl + O` для сохранения
- Нажмите `Enter` для подтверждения
- Нажмите `Ctrl + X` для выхода

### 4.5. Установка прав доступа

```bash
# Устанавливаем правильные права на файлы
cd /var/www/qrshot

# Права на директорию проекта
sudo chown -R $USER:$USER /var/www/qrshot

# Права на директорию uploads (для загруженных файлов)
sudo chmod -R 755 /var/www/qrshot/backend/uploads

# Права на базу данных
sudo chmod 644 /var/www/qrshot/backend/db/*.db 2>/dev/null || true
```

---

## 5. Настройка базы данных

### 5.1. Инициализация базы данных

База данных SQLite создастся автоматически при первом запуске сервера. Но можно проверить:

```bash
# Переходим в директорию backend
cd /var/www/qrshot/backend

# Проверяем наличие файла базы данных (создастся при первом запуске)
ls -la db/
```

### 5.2. Резервное копирование базы данных (настройка)

```bash
# Создаем директорию для бэкапов
mkdir -p /var/backups/qrshot

# Создаем скрипт для автоматического бэкапа
nano /var/www/qrshot/backup-db.sh
```

**Содержимое скрипта бэкапа:**

```bash
#!/bin/bash
BACKUP_DIR="/var/backups/qrshot"
DB_PATH="/var/www/qrshot/backend/db/qr_photoshare.db"
DATE=$(date +%Y%m%d_%H%M%S)

# Создаем бэкап
cp "$DB_PATH" "$BACKUP_DIR/qr_photoshare_$DATE.db"

# Удаляем бэкапы старше 30 дней
find "$BACKUP_DIR" -name "qr_photoshare_*.db" -mtime +30 -delete

echo "Backup created: qr_photoshare_$DATE.db"
```

```bash
# Делаем скрипт исполняемым
chmod +x /var/www/qrshot/backup-db.sh
```

---

## 6. Настройка веб-сервера (Nginx)

### 6.1. Установка Nginx

```bash
# Устанавливаем Nginx
sudo apt install -y nginx

# Проверяем статус
sudo systemctl status nginx
```

**⏱️ Время выполнения:** 1-2 минуты

### 6.2. Настройка конфигурации Nginx

```bash
# Создаем конфигурационный файл для сайта
sudo nano /etc/nginx/sites-available/qrshot
```

**Содержимое конфигурационного файла:**

```nginx
# HTTP сервер (будет перенаправлять на HTTPS после настройки SSL)
server {
    listen 80;
    listen [::]:80;
    server_name qrshot.kz www.qrshot.kz;

    # Корневая директория для статических файлов фронтенда
    root /var/www/qrshot/frontend;
    index index.html;

    # Для Let's Encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    # Временное перенаправление на HTTPS (раскомментируйте после настройки SSL)
    # return 301 https://$server_name$request_uri;
    
    # API запросы проксируем на Node.js сервер
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Увеличиваем таймауты для загрузки файлов
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        
        # Увеличиваем размер загружаемых файлов
        client_max_body_size 100M;
    }

    # Статические файлы (CSS, JS, изображения)
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Загруженные файлы (uploads) проксируем на Node.js
    location /uploads/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Отключаем кэширование для uploads
        proxy_set_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # HTML файлы фронтенда
    location / {
        try_files $uri $uri/ /index.html;
        
        # Отключаем кэширование для HTML
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}
```

**Важно:** В конфигурации указан домен `qrshot.kz`. Если у вас другой домен, замените его.

### 6.3. Активация конфигурации

```bash
# Создаем символическую ссылку
sudo ln -s /etc/nginx/sites-available/qrshot /etc/nginx/sites-enabled/

# Удаляем дефолтную конфигурацию (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверяем конфигурацию на ошибки
sudo nginx -t

# Если проверка прошла успешно, перезагружаем Nginx
sudo systemctl reload nginx
```

**⏱️ Время выполнения:** 1 минута

---

## 7. Настройка HTTPS (Let's Encrypt)

### 7.1. Установка Certbot

```bash
# Устанавливаем Certbot
sudo apt install -y certbot python3-certbot-nginx

# Проверяем установку
certbot --version
```

**⏱️ Время выполнения:** 2-3 минуты

### 7.2. Получение SSL сертификата

**Важно:** Перед выполнением этой команды убедитесь, что:
- Домен указывает на IP адрес вашего сервера
- Порт 80 открыт в файрволе
- Nginx работает и доступен

```bash
# Получаем SSL сертификат
sudo certbot --nginx -d qrshot.kz -d www.qrshot.kz

# Следуйте инструкциям:
# - Введите email для уведомлений
# - Согласитесь с условиями использования
# - Выберите, перенаправлять ли HTTP на HTTPS (рекомендуется: 2)
```

**⏱️ Время выполнения:** 1-2 минуты

### 7.3. Автоматическое обновление сертификата

Certbot автоматически настраивает обновление сертификата. Проверить можно командой:

```bash
# Проверяем статус автообновления
sudo systemctl status certbot.timer

# Тестируем обновление сертификата
sudo certbot renew --dry-run
```

### 7.4. Обновление конфигурации Nginx для HTTPS

После получения сертификата Certbot автоматически обновит конфигурацию Nginx. Проверьте:

```bash
# Просматриваем обновленную конфигурацию
sudo nano /etc/nginx/sites-available/qrshot
```

**Полная конфигурация файла `/etc/nginx/sites-available/qrshot` после получения SSL сертификата:**

```nginx
# HTTP сервер - перенаправляет все запросы на HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name qrshot.kz www.qrshot.kz;

    # Для Let's Encrypt (обновление сертификата)
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    # Перенаправление всех HTTP запросов на HTTPS
    return 301 https://$server_name$request_uri;
}

# HTTPS сервер
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name qrshot.kz www.qrshot.kz;

    # Корневая директория для статических файлов фронтенда
    root /var/www/qrshot/frontend;
    index index.html;

    # SSL сертификаты
    ssl_certificate /etc/letsencrypt/live/qrshot.kz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/qrshot.kz/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # API запросы проксируем на Node.js сервер
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Увеличиваем таймауты для загрузки файлов
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        
        # Увеличиваем размер загружаемых файлов
        client_max_body_size 100M;
    }

    # Статические файлы (CSS, JS, изображения)
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Загруженные файлы (uploads) проксируем на Node.js
    location /uploads/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Отключаем кэширование для uploads
        proxy_set_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # HTML файлы фронтенда
    location / {
        try_files $uri $uri/ /index.html;
        
        # Отключаем кэширование для HTML
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}
```

**Важно:** После обновления конфигурации проверьте и перезагрузите Nginx:

```bash
# Проверяем конфигурацию на ошибки
sudo nginx -t

# Если проверка прошла успешно, перезагружаем Nginx
sudo systemctl reload nginx
```

### 7.5. Обновление переменных окружения

```bash
# Обновляем FRONTEND_URL в .env файле
cd /var/www/qrshot/backend
nano .env
```

Измените:
```env
FRONTEND_URL=https://qrshot.kz
```

---

## 8. Настройка автозапуска (PM2)

### 8.1. Запуск приложения через PM2

**⚠️ ВАЖНО: Не используйте `npm run dev` для продакшена!** 

`npm run dev` использует `nodemon` (инструмент для разработки) и может вызывать ошибки "Permission denied" в продакшене. Для продакшена используйте PM2 или `npm start`.

```bash
# Переходим в директорию backend
cd /var/www/qrshot/backend

# Вариант 1: Запуск напрямую через PM2 (рекомендуется)
pm2 start server.js --name qrshot

# Вариант 2: Запуск через npm start (альтернатива)
# pm2 start npm --name qrshot -- start

# Сохраняем конфигурацию PM2 для автозапуска
pm2 save

# Настраиваем автозапуск PM2 при загрузке системы
pm2 startup

# Выполните команду, которую выведет pm2 startup (она будет выглядеть примерно так):
# sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u qrshot --hp /home/qrshot
```

**Проверка запуска:**

```bash
# Проверяем статус
pm2 status

# Просматриваем логи
pm2 logs qrshot --lines 20

# Проверяем, что сервер отвечает
curl http://localhost:3000/api/events
```

**⏱️ Время выполнения:** 2-3 минуты

### 8.2. Проверка статуса приложения

```bash
# Просматриваем список процессов
pm2 list

# Просматриваем логи
pm2 logs qrshot

# Просматриваем информацию о процессе
pm2 info qrshot

# Мониторинг в реальном времени
pm2 monit
```

### 8.3. Настройка PM2 для продакшена

```bash
# Создаем файл конфигурации PM2
cd /var/www/qrshot/backend
nano ecosystem.config.js
```

**Содержимое `ecosystem.config.js`:**

```javascript
module.exports = {
  apps: [{
    name: 'qrshot',
    script: 'server.js',
    cwd: '/var/www/qrshot/backend',
    instances: 1,
    exec_mode: 'fork',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: '/var/log/pm2/qrshot-error.log',
    out_file: '/var/log/pm2/qrshot-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_memory_restart: '500M',
    watch: false
  }]
};
```

```bash
# Создаем директорию для логов
sudo mkdir -p /var/log/pm2
sudo chown -R $USER:$USER /var/log/pm2

# Перезапускаем приложение с новой конфигурацией
pm2 delete qrshot
pm2 start ecosystem.config.js
pm2 save
```

---

## 9. Настройка файрвола

### 9.1. Настройка UFW (Uncomplicated Firewall)

```bash
# Проверяем статус файрвола
sudo ufw status

# Разрешаем SSH (ВАЖНО! Сделайте это первым, иначе можете потерять доступ)
sudo ufw allow 22/tcp

# Разрешаем HTTP
sudo ufw allow 80/tcp

# Разрешаем HTTPS
sudo ufw allow 443/tcp

# Включаем файрвол
sudo ufw enable

# Проверяем статус
sudo ufw status verbose
```

**⏱️ Время выполнения:** 1 минута

### 9.2. Проверка открытых портов

```bash
# Проверяем, какие порты слушают приложения
sudo netstat -tlnp | grep -E ':(80|443|3000|22)'
```

---

## 10. Проверка работы

### 10.1. Проверка работы Node.js приложения

```bash
# Проверяем, что приложение запущено
pm2 list

# Проверяем логи на ошибки
pm2 logs qrshot --lines 50

# Проверяем, что сервер отвечает
curl http://localhost:3000
```

### 10.2. Проверка работы Nginx

```bash
# Проверяем статус Nginx
sudo systemctl status nginx

# Проверяем логи Nginx
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log
```

### 10.3. Проверка работы через браузер

1. Откройте браузер
2. Перейдите по адресу: `https://qrshot.kz`
3. Проверьте:
   - Главная страница загружается
   - Админ панель доступна
   - Можно загрузить фото/видео
   - QR код генерируется

### 10.4. Проверка HTTPS

```bash
# Проверяем SSL сертификат
openssl s_client -connect qrshot.kz:443 -servername qrshot.kz

# Или используйте онлайн сервисы:
# https://www.ssllabs.com/ssltest/
```

---

## 11. Управление сервисом

### 11.1. Управление приложением через PM2

```bash
# Перезапуск приложения
pm2 restart qrshot

# Остановка приложения
pm2 stop qrshot

# Запуск приложения
pm2 start qrshot

# Удаление приложения из PM2
pm2 delete qrshot

# Просмотр логов
pm2 logs qrshot

# Очистка логов
pm2 flush

# Перезагрузка приложения без простоя (zero-downtime)
pm2 reload qrshot
```

### 11.2. Управление Nginx (Фронтенд)

**Важно:** Фронтенд - это статические файлы, которые обслуживаются через Nginx. Для управления фронтендом нужно управлять Nginx.

**Остановка фронтенда (остановка Nginx):**
```bash
# Останавливаем Nginx (фронтенд станет недоступен)
sudo systemctl stop nginx

# Проверяем статус
sudo systemctl status nginx
```

**Запуск фронтенда (запуск Nginx):**
```bash
# Запускаем Nginx (фронтенд станет доступен)
sudo systemctl start nginx

# Проверяем статус
sudo systemctl status nginx
```

**Перезапуск фронтенда (перезапуск Nginx):**
```bash
# Перезагружаем конфигурацию Nginx без остановки сервиса (рекомендуется)
sudo systemctl reload nginx

# Или полный перезапуск
sudo systemctl restart nginx

# Проверяем статус
sudo systemctl status nginx
```

**Проверка конфигурации перед применением:**
```bash
# Проверяем конфигурацию на ошибки
sudo nginx -t

# Если проверка прошла успешно, применяем изменения
sudo systemctl reload nginx
```

**Просмотр логов Nginx:**
```bash
# Все логи
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Или через journalctl
sudo journalctl -u nginx -f
```

### 11.3. Обновление приложения

```bash
# Переходим в директорию проекта
cd /var/www/qrshot

# Если используете Git:
git pull origin main

# Устанавливаем новые зависимости (если есть)
cd backend
npm install

# Перезапускаем приложение
pm2 restart qrshot

# Проверяем логи
pm2 logs qrshot --lines 20
```

---

## 12. Резервное копирование

### 12.1. Ручное резервное копирование

```bash
# Создаем директорию для бэкапов
mkdir -p /var/backups/qrshot

# Бэкап базы данных
cp /var/www/qrshot/backend/db/qr_photoshare.db /var/backups/qrshot/db_$(date +%Y%m%d_%H%M%S).db

# Бэкап загруженных файлов
tar -czf /var/backups/qrshot/uploads_$(date +%Y%m%d_%H%M%S).tar.gz /var/www/qrshot/backend/uploads

# Бэкап конфигурации
tar -czf /var/backups/qrshot/config_$(date +%Y%m%d_%H%M%S).tar.gz /var/www/qrshot/backend/.env
```

### 12.2. Автоматическое резервное копирование (Cron)

```bash
# Открываем crontab
crontab -e

# Добавляем задачу для ежедневного бэкапа в 2:00 ночи
0 2 * * * /var/www/qrshot/backup-db.sh >> /var/log/qrshot-backup.log 2>&1
```

### 12.3. Восстановление из бэкапа

```bash
# Останавливаем приложение
pm2 stop qrshot

# Восстанавливаем базу данных
cp /var/backups/qrshot/db_YYYYMMDD_HHMMSS.db /var/www/qrshot/backend/db/qr_photoshare.db

# Восстанавливаем загруженные файлы
tar -xzf /var/backups/qrshot/uploads_YYYYMMDD_HHMMSS.tar.gz -C /

# Запускаем приложение
pm2 start qrshot
```

---

## 🔧 Решение проблем

### Проблема: Backend не запускается (Frontend работает, но API не отвечает)

**⚠️ ВАЖНО: Для продакшена используйте PM2, а не `npm run dev`!**

`npm run dev` использует `nodemon` (инструмент для разработки) и не предназначен для продакшена. Для продакшена используйте PM2.

**Шаг 1: Проверка статуса PM2**

```bash
# Проверяем список процессов PM2
pm2 list

# Если приложение не запущено или в статусе "errored", проверяем логи
pm2 logs qrshot --lines 50

# Просматриваем только ошибки
pm2 logs qrshot --err --lines 100
```

**Шаг 1.1: Если пытались запустить через `npm run dev` и получили ошибку "Permission denied"**

```bash
# Остановите все процессы (если запущены)
pm2 stop all
pm2 delete all

# Для ПРОДАКШЕНА используйте PM2:
cd /var/www/qrshot/backend
pm2 start server.js --name qrshot

# Или используйте npm start (без nodemon):
npm start

# Для PM2 это будет:
pm2 start npm --name qrshot -- start
```

**Шаг 2: Проверка порта**

```bash
# Проверяем, что порт 3000 свободен или занят правильным процессом
sudo netstat -tlnp | grep 3000

# Или используем ss
sudo ss -tlnp | grep 3000

# Проверяем, может ли приложение подключиться к порту
curl http://localhost:3000/api/events
```

**Шаг 3: Проверка переменных окружения**

```bash
# Переходим в директорию backend
cd /var/www/qrshot/backend

# Проверяем наличие файла .env
ls -la .env

# Просматриваем содержимое .env (убедитесь, что нет синтаксических ошибок)
cat .env

# Проверяем, что файл читается
node -e "require('dotenv').config(); console.log('PORT:', process.env.PORT);"
```

**Шаг 4: Проверка зависимостей**

```bash
# Проверяем, что все зависимости установлены
cd /var/www/qrshot/backend
npm list --depth=0

# Если есть проблемы, переустанавливаем зависимости
rm -rf node_modules package-lock.json
npm install
```

**Шаг 5: Запуск приложения вручную для диагностики**

```bash
# Останавливаем PM2 процесс (если запущен)
pm2 stop qrshot
pm2 delete qrshot

# Запускаем приложение напрямую для просмотра ошибок
cd /var/www/qrshot/backend
node server.js
```

**Типичные ошибки и решения:**

1. **Ошибка: "invalid ELF header" или "ERR_DLOPEN_FAILED" для sqlite3**
   
   **Причина:** Нативные модули (например, `sqlite3`) были скомпилированы для другой платформы (Windows) и скопированы на Linux. Нативные модули нужно пересобрать на целевой системе.
   
   **Решение:**
   ```bash
   # Останавливаем PM2 процесс
   pm2 stop qrshot
   pm2 delete qrshot
   
   # Переходим в директорию backend
   cd /var/www/qrshot/backend
   
   # Удаляем node_modules и package-lock.json
   rm -rf node_modules package-lock.json
   
   # Убеждаемся, что установлены build tools (если еще не установлены)
   sudo apt-get update
   sudo apt-get install -y build-essential python3
   
   # Переустанавливаем зависимости (они скомпилируются для Linux)
   npm install
   
   # Проверяем, что sqlite3 установился правильно
   node -e "require('sqlite3'); console.log('SQLite3 loaded successfully');"
   
   # Запускаем через PM2
   pm2 start server.js --name qrshot
   pm2 save
   
   # Проверяем логи
   pm2 logs qrshot --lines 20
   ```

2. **Ошибка: "Cannot find module"**
   ```bash
   # Решение: переустановить зависимости
   cd /var/www/qrshot/backend
   npm install
   ```

3. **Ошибка: "Port 3000 already in use"**
   ```bash
   # Решение: найти и остановить процесс, занимающий порт
   sudo lsof -i :3000
   # Или
   sudo fuser -k 3000/tcp
   ```

4. **Ошибка: "EACCES: permission denied"**
   ```bash
   # Решение: проверить права доступа
   sudo chown -R $USER:$USER /var/www/qrshot
   sudo chmod -R 755 /var/www/qrshot/backend/uploads
   ```

5. **Ошибка: "Database file is locked"**
   ```bash
   # Решение: проверить права на базу данных
   sudo chmod 644 /var/www/qrshot/backend/db/*.db
   sudo chown $USER:$USER /var/www/qrshot/backend/db/*.db
   ```

6. **Ошибка: "JWT_SECRET is not defined"**
   ```bash
   # Решение: проверить .env файл
   cd /var/www/qrshot/backend
   # Убедитесь, что JWT_SECRET указан в .env
   grep JWT_SECRET .env
   ```

**Шаг 6: После исправления ошибок - перезапуск через PM2**

```bash
# Перезапускаем приложение через PM2
cd /var/www/qrshot/backend
pm2 start server.js --name qrshot

# Или если используете ecosystem.config.js
pm2 start ecosystem.config.js

# Сохраняем конфигурацию
pm2 save

# Проверяем статус
pm2 status
pm2 logs qrshot --lines 20
```

### Проблема: Приложение не запускается

```bash
# Проверяем логи PM2
pm2 logs qrshot --err

# Проверяем, что порт 3000 свободен
sudo netstat -tlnp | grep 3000

# Проверяем переменные окружения
cd /var/www/qrshot/backend
cat .env
```

### Проблема: Nginx возвращает 502 Bad Gateway

```bash
# Проверяем, что приложение запущено
pm2 list

# Проверяем логи Nginx
sudo tail -f /var/log/nginx/error.log

# Проверяем, что приложение слушает на порту 3000
curl http://localhost:3000
```

### Проблема: Не работает HTTPS

```bash
# Проверяем сертификат
sudo certbot certificates

# Обновляем сертификат вручную
sudo certbot renew

# Проверяем конфигурацию Nginx
sudo nginx -t
```

### Проблема: Ошибки при установке зависимостей

```bash
# Очищаем кэш npm
npm cache clean --force

# Удаляем node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Переустанавливаем зависимости
npm install
```

### Проблема: Недостаточно прав доступа

```bash
# Проверяем владельца файлов
ls -la /var/www/qr-photoshare

# Устанавливаем правильного владельца
sudo chown -R $USER:$USER /var/www/qrshot

# Устанавливаем права на директорию uploads
sudo chmod -R 755 /var/www/qrshot/backend/uploads
```

---

## 📊 Мониторинг и производительность

### Мониторинг через PM2

```bash
# Мониторинг в реальном времени
pm2 monit

# Статистика использования ресурсов
pm2 status

# Детальная информация
pm2 show qrshot
```

### Мониторинг системных ресурсов

```bash
# Использование CPU и памяти
htop

# Использование диска
df -h

# Использование памяти
free -h

# Логи системы
sudo journalctl -xe
```

---

## 🔒 Безопасность

### Рекомендации по безопасности

1. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

2. **Используйте сильные пароли** для администратора в `.env`

3. **Ограничьте доступ к SSH:**
   ```bash
   # Измените порт SSH (опционально)
   sudo nano /etc/ssh/sshd_config
   # Port 2222
   sudo systemctl restart sshd
   ```

4. **Настройте fail2ban** для защиты от брутфорса:
   ```bash
   sudo apt install -y fail2ban
   sudo systemctl enable fail2ban
   sudo systemctl start fail2ban
   ```

5. **Регулярно делайте бэкапы** базы данных и загруженных файлов

6. **Мониторьте логи** на подозрительную активность

---

## ✅ Чек-лист после установки

- [ ] Node.js установлен и работает
- [ ] Зависимости проекта установлены
- [ ] Файл `.env` настроен с правильными значениями
- [ ] База данных создана и работает
- [ ] Nginx установлен и настроен
- [ ] SSL сертификат получен и работает
- [ ] PM2 настроен и приложение запущено
- [ ] Файрвол настроен
- [ ] Приложение доступно через браузер
- [ ] HTTPS работает корректно
- [ ] Автозапуск настроен
- [ ] Бэкапы настроены

---

## 📞 Полезные команды

```bash
# Просмотр всех запущенных процессов PM2
pm2 list

# Просмотр логов в реальном времени
pm2 logs qrshot

# Перезагрузка приложения
pm2 restart qrshot

# Проверка статуса Nginx
sudo systemctl status nginx

# Перезагрузка Nginx
sudo systemctl reload nginx

# Просмотр логов Nginx
sudo tail -f /var/log/nginx/error.log

# Проверка открытых портов
sudo netstat -tlnp

# Проверка использования диска
df -h

# Проверка использования памяти
free -h
```

---

## 🎉 Готово!

Ваш сервер QRShot успешно развернут на Ubuntu 22.04 LTS!

Если возникнут вопросы или проблемы, проверьте логи:
- PM2: `pm2 logs qrshot`
- Nginx: `sudo tail -f /var/log/nginx/error.log`
- Система: `sudo journalctl -xe`

